mutation attachImageToBoard($id: ID!, $imageId: ID!) {
  boardUpdate(
    filter: { id: $id }
    data: { image: { reconnect: { id: $imageId } } }
  ) {
    id
    imageUrl {
      id
      downloadUrl
    }
  }
}
